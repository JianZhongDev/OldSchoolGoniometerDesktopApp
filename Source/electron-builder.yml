appId: com.jianzhong.goniometeroverlay
productName: Goniometer Overlay
directories:
  output: ../Apps
  buildResources: build
files:
  - out/**/*
  - package.json
asarUnpack:
  - resources/**

win:
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
  icon: build/icon.ico

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true

mac:
  target:
    - target: dmg
      arch: [universal]
  # A ready-made build/icon.icns is preferred, but if it's absent electron-builder
  # generates the .icns from this PNG on the macOS runner (requires >=512px; ours
  # is 1024x1024).
  icon: build/icon.png
  category: public.app-category.graphics-design
  extendInfo:
    LSUIElement: true
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
