# Build outputs (regenerable — produced by electron-vite / electron-builder).
# Installers are published to GitHub Releases via .github/workflows/release.yml,
# NOT committed to the repo (keeps clones small and history clean).
/Apps/
/AppsBuild/
dist/

# Dependencies and build caches (also covered by Source/.gitignore)
node_modules/
out/
*.log
.DS_Store
.vite/
